[project]
name = "sales-retail-dashboard"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "dash>=3.0.0",
    "plotly>=6.0.0",
    "pandas>=2.0.0",
    "numpy>=1.24.0",
    "gunicorn>=20.1.0"
]

[tool.vercel]
entrypoint = "dashboard_fixed:app"
